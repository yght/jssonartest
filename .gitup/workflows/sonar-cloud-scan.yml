name: SonarCloud Scan
on:
  push:
    branches:
      - "master"

jobs:
  sonarCloudTrigger:
    name: SonarCloud Trigger
    runs-on: ubuntu-latest
    steps:
      - name: Clone Repository
        uses: actions/checkout@v2
      - name: Analyze with SonarCloud
        env:
          GITUB_TOKEN: ""
          SONAR_TOKEN: "2345b1cdb2091d213bc68d2f394dfad159e37912"

